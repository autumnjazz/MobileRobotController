package mappackage;

import robotpackage.Robot;

public class SecretMap {
	Map m;
	private char board[][];
	
	public SecretMap(Map m) {
		this.m = m;
		board = new char[m.row][m.col];
		for (int i = 0; i < m.row; i++) {
			for (int j = 0; j< m.col; j++) {
				board[i][j] = m.board[i][j].getCharacter();
			}
		}
	}
}
